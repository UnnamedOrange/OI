# Items

1. 想要让某个后缀的字典序最大，这个后缀的的一个字符的字典序必须最大（或相对最大）。否则它的字典序比以字典序最大的字符开头的后缀小。
2. 在后缀数组中与待计算项相邻的且 $height \ge 1$ 的是可能字典序比待计算项大的。由 1.，其它项的第一个字符都小于待计算项。
3. 我们一开始会确定一个字符，即字典序最大的字符。先比较待计算项是否已经是最大的，或者已经不可能是最大的。方法是看每个以该字符开头的后缀有开头有多少个连续的该字符。
4. 若没有确定，

